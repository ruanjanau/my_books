import 'package:flutter/material.dart';

class TextInfoTile extends StatelessWidget {
  const TextInfoTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
